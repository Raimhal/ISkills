import {createTheme} from "@mui/material/styles";

export const colorTheme = createTheme({
    palette: {
        primary: {
            main: '#975ad4',
        },
        secondary: {
            main: '#9c27b0',
        },
    },
});